bracket the baud rate it accepts

find transmitter ready bit instead of just sleeping

maybe play with uncommitted bits in packet

decide if all up code is useful

experiment with light dimming feature

experiment with power setting
